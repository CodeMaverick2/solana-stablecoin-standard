{
  "name": "@stbr/solana-stablecoin-standard",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "sdk/core",
    "backend"
  ],
  "scripts": {
    "build": "anchor build && npm run build:sdk",
    "build:sdk": "npm run build --workspace=sdk/core",
    "test": "anchor test",
    "test:sss1": "anchor test --skip-build --run tests/sss-1.ts",
    "test:sss2": "anchor test --skip-build --run tests/sss-2.ts",
    "test:sss3": "anchor test --skip-build --run tests/sss-3.ts",
    "test:lifecycle": "anchor test --skip-build --run tests/full-lifecycle.ts",
    "test:compliance": "anchor test --skip-build --run tests/compliance.ts",
    "test:roles": "anchor test --skip-build --run tests/roles.ts",
    "test:edge": "anchor test --skip-build --run tests/edge-cases.ts",
    "test:token-extensions": "anchor test --skip-build --run tests/token-extensions.ts",
    "clean": "rm -rf target node_modules dist .anchor test-ledger"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.31.1",
    "@solana/spl-token": "^0.4.10",
    "@solana/web3.js": "^1.98.0"
  },
  "devDependencies": {
    "@types/chai": "^4.3.11",
    "@types/mocha": "^10.0.6",
    "chai": "^4.4.1",
    "mocha": "^10.3.0",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.7.3"
  }
}
